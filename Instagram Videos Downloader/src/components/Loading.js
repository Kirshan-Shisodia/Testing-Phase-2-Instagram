import React from "react";
import ReactLoading from "react-loading";

const Loading = () => (
  <div>
    <ReactLoading type="bars" color="black" height={100} width={70} />
  </div>
);

export default Loading;
